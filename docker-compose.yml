version: '3.7'

services:
  api-telegram:
    image: $AND_DOCKER_REGISTRY_NAME/$PROGECT-$APP:dev
    command: ./rpg-api-telegram
    tty: true
    container_name: api-telegram
    ports:
      - 5001:5001
      - 5432:5432
    restart: always
